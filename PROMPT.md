you are building a zig libsql adapter. all of our toolchains and runtimes are written in pure(as) zig and we would like to keep it that way.

in the desire to self host if you have to build a rust project here use rusty (rusty init lib -cli should work) which is an in house product that will usse this adapter
